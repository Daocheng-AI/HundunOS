# Grafana 仪表盘配置
apiVersion: 1

providers:
  - name: 'HundunOS'
    orgId: 1
    folder: 'HundunOS'
    folderUid: ''
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
